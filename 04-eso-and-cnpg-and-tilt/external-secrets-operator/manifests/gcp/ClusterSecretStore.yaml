apiVersion: external-secrets.io/v1
kind: ClusterSecretStore
metadata:
  name: gcp-store
spec:
  provider:
    gcpsm:
      projectID: k8s-learning-gke
      auth:
        workloadIdentity:
          clusterLocation: europe-central2-a
          clusterName: klaus-learning
          serviceAccountRef:
            name: external-secrets
            namespace: external-secrets