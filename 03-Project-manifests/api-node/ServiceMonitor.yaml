apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: api-node
  namespace: demo-app
  labels:
    release: kube-prometheus-stack
spec:
  selector:
    matchLabels:
      app: api-node        # must match Service's selector labels
  endpoints:
    - port: http              # must match Service.yaml's spec.ports[].name exactly
      path: /metrics
      interval: 30s