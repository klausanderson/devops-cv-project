apiVersion: v1
kind: Service
metadata:
  name: api-node
  namespace: demo-app
  labels: 
    app: api-node
spec:
  selector:
    app: api-node
  ports:
    - name: http
      protocol: TCP
      port: 3000
      targetPort: 3000
