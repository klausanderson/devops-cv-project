apiVersion: v1
kind: Service
metadata:
  name: api-golang
  namespace: demo-app
  labels: 
    app: api-golang
spec:
  selector:
    app: api-golang
  ports:
    - name: http
      protocol: TCP
      port: 8000
      targetPort: 8000
